name: WhatsApp
run-name: WhatsApp Bot
on:
  push:
    branches: [ main ]

jobs:
  setup:
    name: initialize
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          run: node -v